{
  "name": "node-minify",
  "description": "Monorepo for node-minify",
  "homepage": "https://github.com/srod/node-minify",
  "author": {
    "name": "Rodolphe Stoclin",
    "email": "rodolphe@2clics.net",
    "url": "http://2clics.net"
  },
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/srod/node-minify.git"
  },
  "scripts": {
    "setup": "npm run clean:packages && yarn && npm run bootstrap && npm run build",
    "bootstrap": "lerna bootstrap",
    "build": "npm run clean:build && npm run build-js",
    "build-js": "lerna exec --parallel -- babel --root-mode upward src --out-dir lib --copy-files",
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "eslint": "eslint --ignore-path .gitignore packages",
    "lint": "npm run eslint || true",
    "pretest": "npm run build && npm run eslint",
    "test": "jest --maxWorkers=4",
    "posttest": "npm run clean:coverage",
    "clean:build": "rimraf packages/*/lib",
    "clean:coverage": "rimraf tests/tmp/*.{js,js.map,css,html}",
    "clean:packages": "rimraf node_modules packages/*/node_modules",
    "release-patch": "lerna version patch --tag-version-prefix ''",
    "release-minor": "lerna version minor --tag-version-prefix ''",
    "release-major": "lerna version major --tag-version-prefix ''",
    "release-next-major": "lerna version premajor --preid beta --tag-version-prefix ''",
    "release-next-prerelease": "lerna version prerelease --preid beta --tag-version-prefix ''",
    "showpack": "lerna pack --dry",
    "publish-latest": "NPM_CONFIG_OTP=xxx lerna publish from-git",
    "publish-next": "NPM_CONFIG_OTP=xxx lerna publish from-git --npm-tag next",
    "prettier": "prettier",
    "prettier-write": "npm run prettier -- --write",
    "prettier-project": "npm run prettier-write -- '{bin,lib,__tests__}/**/*.js'"
  },
  "devDependencies": {
    "@babel/cli": "7.1.5",
    "@babel/core": "7.1.6",
    "@babel/preset-env": "7.1.6",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "23.6.0",
    "babel-preset-env": "1.7.0",
    "codecov": "3.1.0",
    "eslint": "5.9.0",
    "eslint-config-prettier": "3.3.0",
    "eslint-plugin-prettier": "3.0.0",
    "husky": "1.2.0",
    "jest": "23.6.0",
    "lerna": "3.5.1",
    "lint-staged": "8.1.0",
    "prettier": "1.14.3",
    "rimraf": "2.6.2"
  },
  "lint-staged": {
    "*.js": [
      "npm run prettier-write --",
      "git add"
    ]
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 120
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "workspaces": [
    "packages/*"
  ],
  "jest": {
    "collectCoverage": true,
    "coverageDirectory": "<rootDir>/coverage/",
    "notify": true,
    "projects": [
      "<rootDir>/packages/*"
    ],
    "rootDir": "./",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      ".history"
    ],
    "verbose": true
  }
}
